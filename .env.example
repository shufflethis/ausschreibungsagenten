# Public build-time value, never a secret. Login, confirmation and account use this one origin.
VITE_APP_BASE_URL=http://localhost:8000

# Server-side Vercel Functions only. Configure in Vercel, do not expose as VITE_*.
AGENTLEADS_API_BASE=http://127.0.0.1:8767
